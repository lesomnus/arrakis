{
	"name": "arrakis-web",
	"private": true,
	"type": "module",
	"scripts": {
		"index": "cd .. && go run . render --kind index > web/index.json",
		"serve": "python3 -m http.server -d . 8000",
		"bench": "node bench/run.mjs",
		"shot": "node tools/shot.mjs",
		"check": "node bench/run.mjs --check"
	},
	"devDependencies": {
		"playwright": "^1.56.0"
	}
}
